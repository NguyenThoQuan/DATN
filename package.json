{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start --prefix main\" \"npm run start:standalone --prefix navbarR\" \"npm run start:standalone --prefix banner\" \"npm run serve:standalone --prefix myservice\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "postcss": "^8.5.3"
  }
}
